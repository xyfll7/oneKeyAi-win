<?xml version="1.0" encoding="utf-8"?>
<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:tb="using:H.NotifyIcon"
    >
    <tb:TaskbarIcon
        x:Key="TrayIcon"
        Visibility="Visible"
        ToolTipText="ToolTip"
        ContextMenuMode="SecondWindow"
        LeftClickCommand="{Binding ShowHideWindowCommand}"
        NoLeftClickDelay="True"
        IconSource="\Assets\Logo.ico"
        >
        <tb:TaskbarIcon.ContextFlyout>
            <MenuFlyout>
                <MenuFlyoutItem Command="{Binding TestCommand}" Text="Test"/>
                <MenuFlyoutSeparator />
                <MenuFlyoutItem Command="{Binding ShowHideWindowCommand}" Text="ShowHide"/>
                <MenuFlyoutSeparator />
                <MenuFlyoutItem Command="{Binding ExitApplicationCommand}" Text="Exit"/>
            </MenuFlyout>
        </tb:TaskbarIcon.ContextFlyout>
    </tb:TaskbarIcon>
</ResourceDictionary>
